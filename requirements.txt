opencv-python-headless==5.0
pandas-stubs==3.0.3
ultralytics==8.4.98
ncnn==1.0.2
Flask==3.1.3
numpy

# Núcleo web
flask>=3.1
flask-login>=0.6
werkzeug>=3.1
cryptography>=43.0

# Inferencia (solo necesarias en el Raspberry Pi 4 con la cámara conectada)
ultralytics>=8.0
opencv-python>=4.9
ncnn>=1.0

# Nota: picamera2 NO va aquí — se instala vía `apt`, no `pip`, y el venv
# debe crearse con `--system-site-packages` para heredarlo. Ver README.

# Pruebas
pytest>=8.0
pytest-cov>=5.0
